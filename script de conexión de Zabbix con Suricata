SCRIPT que da acceso a zabbix para poder leer los json de suricata
/etc/zabbix/scripts/count_suricata_alerts.sh
#!/bin/bash

LOG_FILE="/var/log/suricata/eve.json"

if [ ! -f "$LOG_FILE" ]; then
  echo 0
  exit 0
fi

COUNT=$(grep -c '"event_type":"alert"' "$LOG_FILE" 2>/dev/null)
echo "$COUNT"

